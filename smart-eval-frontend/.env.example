# Smart-Eval AI Frontend — environment variables
# Copy this file to `.env` and adjust values for your environment.

# Base URL of the running Flask backend (no trailing slash; do NOT include /api/v1)
VITE_API_BASE_URL=http://localhost:3000
